import React, { Component } from 'react';
import Matches from '../components/Matches';

class Root extends Component {
  render() {
    return (
      <div className="Root">
        <Matches />
      </div>
    );
  }
}

export default Root;
